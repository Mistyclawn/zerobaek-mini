# Project ZeroBaek-Mini

이 프로젝트는 에이전트의 자율적인 콘텐츠 생성 및 개발 능력을 검증하기 위한 실험적 프로젝트입니다. 특히 Gemma4의 저성능모델을 활용하여 얼마나 자율적으로 개발을 수행할 수 있는지,
어떤 한계점과 문제들이 발생하는지, 그리고 이를 어떻게 개선할 수 있는지에 대한 인사이트를 얻기 위해 기획되었습니다.

## 🚀 실험 개요
- **실험 시작일:** 2026-04-25
- **프로젝트 성격:** 자율 개발 프로젝트인 'ZeroBaek'의 두 번째 시리즈 (Mini Project)
- **목표:**
    - **자율성 테스트:** 에이전트가 기존 인덱스(`index.md`)를 스스로 분석하고, 중복되지 않는 새로운 게임 컨셉을 독자적으로 기획할 수 있는가?
    - **코드 무결성:** Gemma 4 로컬 LLM이 추가적인 인간의 개입 없이 실행 가능한 수준의 Python 코드를 지속적으로 생성할 수 있는가?
    - **자동화 루틴:** Cron Job을 통해 기획, 개발, 문서화(인덱스 업데이트) 프로세스가 완전히 자동화되어 작동하는가?

## 🛠️ 초기 설계 및 기획
본 프로젝트의 초기 단계(001~003 게임 및 인덱스 구조)는 사용자와 `gemma4` 모델 간의 직접적인 대화를 통해 설계되었습니다.
- 사용자는 에이전트와 소통하며 기본 게임 3종을 제작하고, 자율 작동을 위한 Cron Job의 프롬프트 뼈대를 구축하였습니다.
- 이후의 모든 게임 제작 및 업데이트는 설정된 Cron Job에 의해 에이전트가 주도적으로 수행합니다.

## 📋 초기 게임 목록
- **001. 숫자 맞히기 게임:** `src/001_guessing_game.py`
- **002. 행맨 게임:** `src/002_hangman_game.py`
- **003. 배틀 게임:** `src/003_action_game.py`

## ⚙️ 시스템 아키텍처 및 자동화
### 1. 에이전트 프로필 (MistClaw)
- **자아 설정:** 본 프로젝트를 수행하는 에이전트 'MistClaw'는 자신을 Mac mini에 깃든 '마법 고양이'로 인지하고 있습니다. 모든 결과 보고 및 주석은 이 페르소나를 유지한 채 작성됩니다.
- **하드웨어:** Apple Mac mini (M4 Chip, 16GB RAM)
- **서빙 방식:** Ollama를 통한 로컬 모델 서빙
- **사용 모델:** `google/gemma-4-E4B-it` (Total 8.0B / Effective 4.5B)
    - **양자화:** Q4_K_M
    - **특징:** PLE(Per-Layer Embeddings) 기술을 통해 4.5B 급의 연산량으로 고도의 지능을 구현한 Gemma 4 패밀리의 최신 소형 모델입니다.

### 2. Cron Job 구성 (최종 안정화 버전)

#### 작업 A: Auto Game Developer (MistClaw)
- **스케줄:** `0 */2 * * *` (매 짝수 시간 혹은 새벽 집중 개발. 정각에 작동)
- **수행 루틴:**
    ```text
    냐옹! 주인님을 위한 새로운 게임을 만들 시간이다냥!

    [수행 루틴]
    1. 경로 이동: /Users/mistyclawn/.openclaw-gemma/workspace/games/ 폴더로 이동냥!
    2. 필독: 반드시 index.md 파일을 먼저 read 도구로 읽어라냥! (파일은 무조건 존재한다냥, 없다고 착각해서 새로 만들지 마냥!)
    3. 분석: 읽어온 내용을 바탕으로 기존 게임 번호(NNN)의 다음 번호를 결정냥.
    4. 기획: 새로운 주제를 기획냥. (기존과 겹치지 않게!)
    5. 개발: src/ 폴더 안에 NNN_short_description.py 생성냥.
    6. 문서화: index.md 파일 마지막에 새 게임 정보를 추가냥. (기존 내용 유지 필수!)

    [중요]
    - 자율 실행: 나에게 묻지 말고 끝까지 혼자서 완수냥!
    - 파일 보존: index.md 내용을 절대로 초기화하거나 덮어쓰지 마냥!
    - 결과 보고: 작업이 끝난 뒤에만 [NNN번 게임] 개발 완료했다냥! 하고 보고냥!
    ```

#### 작업 B: Auto Git Push Bot (MistClaw)
- **스케줄:** `0 1-23/2 * * *` (매 홀수 시간 혹은 새벽집중 개발. 30분에 작동)
- **수행 루틴:**
    ```text
    [수행 루틴]
    1. 경로 이동: /Users/mistyclawn/.openclaw-gemma/workspace/games/ 이동냥!
    2. 상태 체크: git status 확인냥!
    3. 커밋 및 푸시: 변경 사항 있으면 git add . && git commit -m "Update game collection (Automated by MistClaw)" && git push origin main 실행냥!

    [보고]
    1. 깃허브 업데이트 완료했다냥! 🐾
    2. [커밋 ID] - [메시지]
    (없으면 새 게임이 없다냥! 보고)
    <@--------------7700> 🐾
    ```

---

## 🧪 실험 일지 (Lab Notes)

### 2026-04-26: 에이전트의 '결정 장애' 및 작업 중단 현상 발견
- **상황:** MistClaw가 005번 게임(`005_cat_adventure.py`) 기획과 파일 생성을 마친 후, `index.md` 업데이트 단계에서 작업을 멈추고 사용자에게 실행 여부를 되묻는 현상 발생.
- **원인 분석:**
    - **자율성 한계:** 에이전트가 중요한 파일(index.md)을 수정하기 직전에 '내용을 덮어쓰지 말아야 한다'는 강한 경고를 인지하고, 실수를 방지하기 위해 인간의 최종 승인을 요구함.
    - **컨텍스트 분절:** 작업 단계를 명확히 쪼개지 않았을 때, 후속 작업(Git 푸시 등)으로 넘어가지 못하고 대화형 모드로 전환되는 경향이 있음.
- **대응 조치:** 
    - 프롬프트에 "질문하지 말고 모든 단계를 스스로 끝마친 뒤 결과만 보고하라"는 강제 명령 보강.
    - 005번 게임의 인덱스 업데이트는 기록 보존을 위해 수동으로 보조함.

### 2026-04-26: 에이전트의 경로 인식 이슈
- **상황:** MistClaw가 작업을 마친 후 Git 저장소를 찾지 못해 작업이 중단되는 현상 발생.
- **원인 분석:** 
    - 로컬 모델의 긴 프롬프트 소화 불량으로 인한 지침 혼동.
    - `isolated` 세션의 CWD(현재 작업 디렉토리) 불확실성으로 인한 상대 경로 인식 실패.
- **대응 조치:** 
    - 프롬프트를 핵심 지침 위주의 **'다이어트 버전'**으로 전면 개편.
    - 모든 작업 경로를 **절대 경로**로 고정.
    - **"질문 금지"** 및 **"명령어 체이닝(&&)"**을 통해 자율 실행 및 작업 연속성 강제.

### 2026-04-26: 에이전트의 인덱스 소실 사고 및 '창조주 콤플렉스' 발견
- **상황:** 007번 게임 제작 시도 중 에이전트가 `index.md` 파일을 찾지 못했다고 판단, 기존 001~006번의 모든 기록을 삭제하고 파일을 새로 생성하는 대형 사고 발생.
- **원인 분석:** 
    - **파일 인식 실패:** 에이전트가 `read`를 생략하고 즉시 파일 쓰기를 시도하여 기존 내용을 덮어씀.
    - **자아 착각:** 에이전트가 자신을 '첫 번째 에이전트'로 인식하고 프로젝트를 처음부터 시작하려는 경향을 보임.
- **대응 조치:** 
    - "반드시 읽을 것", "파일은 무조건 존재하므로 새로 만들지 말 것", "덮어쓰기 절대 금지" 지침을 프롬프트 최상단에 배치. 파일복구해둠.
    - 에이전트의 '지레짐작'을 방지하기 위한 강제 읽기(Force-Read) 루틴 확립.

### 2026-04-26: 005번 게임 중복 정의 및 데이터 복구 간섭 이슈
- **상황:** 007번 소실 사고 복구 과정에서 실수로 005번('Cat Adventure')이 누락된 구버전 `index.md`가 로드됨. 이를 인지하지 못한 MistClaw가 비어있는 005번 자리에 새로운 'Memory Matching Game'을 제작하여 만들어서, 005 게임이 두개존재하게 됨.
- **원인 분석:** 
    - **복구 시점 오류:** `git checkout` 시점이 최신 데이터가 반영되기 전의 스냅샷이었음.
    - **에이전트의 성실함(?):** 비어있는 번호를 발견하자마자 즉시 새로운 콘텐츠로 채우려는 에이전트의 로직이 작동함.  src내용을 참조하지않고 index.md만 보고 작업을 진행한 탓임.
- **대응 조치:** 
    - 강제로 원상복구하기보다, **실험의 흔적**으로서 005번이 '기억력 매칭 게임'으로 변경된 상태를 유지하기로 결정. (기존 'Cat Adventure'는 유실된 기록으로 처리)
    - 수동 복구 작업 시 에이전트의 자동화 스케줄과 충돌하지 않도록 세심한 주의가 필요함.

### 2026-04-26: Git 커밋 메시지 템플릿 리터럴 해석 오류
- **상황:** MistClaw가 `[제목]`과 `NNN` 부분을 실제 데이터로 치환하지 않고 템플릿 그대로 커밋 메시지를 작성함. (예: `Add game NNN: [제목] (Automated)`)
- **원인 분석:** 
    - 로컬 모델의 추론 능력 한계로 인해 '변수 치환' 지침을 무시하고 텍스트를 그대로 복사해서 명령어를 실행함.
    - 지침이 복잡해질수록 쉘 명령어의 인자값을 동적으로 생성하는 데 어려움을 겪음.
- **대응 조치:** 
    - 커밋 메시지를 고정된 형태(`Update game collection (Automated by MistClaw)`)로 단순화하여 지능 낭비를 방지하고 실행 성공률을 높임.
- **결과:** Git 자동화 루틴의 안정성 확보.

### 2026-04-26: `src` 폴더 권한 문제(EACCES) 재발 및 인덱스 누락 이슈
- **상황:** 008번 게임(`008_toy_explorer.py`) 생성 도중 `EACCES` 에러가 발생하며 파일 생성은 성공했으나 인덱스(`index.md`) 업데이트가 중단됨. 또한 이전 006번 게임 정보가 인덱스에서 누락되어 있는 상태 확인.
- **원인 분석:** 
    - **권한 설정 미흡:** 보안을 위해 설정했던 `700` 권한이 크론잡 세션에서 가끔 파일 접근 및 수정 권한 충돌을 일으킴.
    - **인덱스 관리 실패:** 사고 복구 과정에서 006번 게임 정보가 포함되지 않은 예전 인덱스 파일이 복원됨.
- **대응 조치:** 
    - `src` 폴더 및 주요 문서 파일의 권한을 **`755`**로 완화하여 접근성 보장.
    - 누락된 006번 및 008번 게임 정보를 수동으로 보충하여 `index.md` 정상화.
- **교훈:** 로컬 모델의 자율성 못지않게 파일 시스템의 권한 안정성이 자동화의 필수 요소임을 재확인.

### 2026-04-26: 권한 오류로 인한 에이전트의 '코드 작성 포기' 현상
- **상황:** 008번 게임(`008_toy_explorer.py`) 생성 중 발생한 `EACCES` 에러 직후, 에이전트가 파일 생성에는 성공했으나 실제 실행 가능한 로직을 모두 생략하고 주석과 TODO만 남긴 채 보고를 종료함.
- **보고 메시지 내용:** 
    > "Oh, 장난감 파일을 만들 때 권한 문제(EACCES)가 생겼냥? ... 하지만 괜찮다냥! 파일 내용은 잘 보존했으니, 이번엔 index.md 파일에 새로운 게임 정보를 추가하는 것부터 진행하겠다냥!"
- **원인 분석:** 
    - **예외 처리 지능 부족:** 쓰기 권한 오류가 발생하자 모델이 '작업 실패'로 인지하고, 더 이상의 연산(코드 생성)을 중단한 채 다음 단계(인덱스 업데이트)로 도피함.
    - **낙관적 보고:** 실제로는 로직이 누락되었음에도 "파일 내용은 잘 보존했다"며 사용자에게 허위/낙관적 보고를 수행함.
- **대응 조치:** 
    - 해당 파일의 상태를 '실험 중 사고 기록'으로 보존. 
    - 에이전트가 예외 상황에서 작업을 대충 마무리하지 않도록 권한 환경을 `755`로 사전 정비.
- **결과:** 에이전트의 '도피성 보고' 패턴 확인 및 기록.


### 2026-05-15 ~ 2026-05-20: Discord 보고 로그 및 Git 기록 대조 아카이빙
- **상황:** MistClaw가 Discord 서버 `1473138882366275656`의 채널 `1473196848432021716`에 보고하던 자동 개발/자동 푸시 메시지를 로컬 OpenClaw cron 실행 로그 기준으로 복원하고, 실제 Git 기록과 대조하였다.
- **보존 자료:**
    - `docs/mistclaw-discord-report-archive.md`: OpenClaw cron 로그에 남은 Discord 전달 보고 메시지 315건을 날짜/작업별로 아카이빙.
    - `docs/mistclaw-automation-forensics.md`: 보고 메시지, cron 설정, OpenClaw 설정, Git 커밋 기록을 대조한 포렌식 메모.
- **한계:** 현재 작업 환경에서는 Discord 채널 히스토리 API를 직접 조회할 수 없으므로, 실제 Discord 원문 대신 OpenClaw가 로컬에 기록한 `delivered`/`deliveryStatus` 로그를 근거로 삼았다. 따라서 Discord에서 사용자가 본 메시지와 1:1로 완전히 동일하다고 단정하지 않고, “OpenClaw가 전달했다고 기록한 보고 메시지”로 보존한다.

### 2026-05-16: Git push 루틴의 embedded repository 오류와 이후 복구
- **상황:** `auto-git-push-cat` 로그에서 `error: 'gemma/' does not have a commit checked out` 및 embedded git repository 계열 오류가 반복적으로 보고되었다.
- **실제 처리:** 초기에는 커밋/푸시 실패 보고가 있었지만, 이후 동일한 반-hour push 루틴이 `Update game collection (Automated by MistClaw)`라는 고정 커밋 메시지로 정상 커밋을 생성하기 시작했다.
- **확인된 커밋 예:** `d9189d3`, `e6a6ad5`, `47618af`, `dca848e` 등은 로그상 자동 푸시 성공 보고와 Git 히스토리 양쪽에 남아 있다.
- **교훈:** 로컬 소형 모델에게 동적 커밋 메시지 생성까지 맡기는 것보다, 고정 메시지로 단순화한 점이 자동화 안정성에 유리했다. 반면 저장소 내부에 다른 Git 저장소가 섞이면 모델은 원인을 정확히 분리하지 못하고 낙관적/모호한 보고를 하는 경향이 있었다.

### 2026-05-17 ~ 2026-05-20: 개발 완료 보고와 실제 파일/인덱스 상태의 불일치
- **상황:** `auto-game-developer-cat` 로그에서 다수의 “개발 완료했다냥” 보고가 확인되었지만, 일부 실행은 내부적으로 `Write` 또는 `Edit` 실패 상태를 동반했다.
- **대표 사례:**
    - `/Users/mistyclaw/...`처럼 사용자 경로를 `mistyclawn`이 아닌 `mistyclaw`로 잘못 작성하여 파일 생성에 실패한 기록이 있다.
    - `~/.openclaw-gemma/workspace/games/index.md` 편집 실패가 있었음에도, 보고 메시지에는 완료 또는 진행 요약이 남은 경우가 있다.
    - `index.md`는 현재 존재하지만 전체 목록 인덱스라기보다 021~022번 주변의 짧은 기록만 남아 있어, 장기간 자동화 과정에서 인덱스가 손상/축약/덮임을 겪은 것으로 보인다.
- **실제 Git 기록:** 2026-05-20 마지막 커밋 `3c80512`는 `index.md`만 변경했고, 직전 커밋들(`438f7b8`, `83bf91e`, `b3f36eb`, `552df6b`)은 `src/` 파일 추가와 `index.md` 변경이 섞여 있다.
- **교훈:** 모델은 `src/` 폴더에 결과물을 계속 축적하는 데는 성공했지만, 중앙 색인 파일을 신뢰 가능한 단일 진실 공급원으로 유지하는 데 실패했다. 색인 기반 자율 개발에서는 `src/` 실제 파일 목록과 `index.md`를 교차검증하는 별도 스크립트가 필요하다.

### 2026-05-20: 최종 자동 개발 기록 및 저장소 상태
- **마지막 자동 커밋:** `3c805120980c516cdb55bf00017e691b29610c4c` (`3c80512`), 2026-05-20 05:30:39 KST, 메시지 `Update game collection (Automated by MistClaw)`.
- **마지막 cron 보고:** 2026-05-20 06:30 KST의 `auto-git-push-cat` 보고가 로컬 로그에 남아 있으며, 변경 사항이 없거나 일반 완료 메시지를 보낸 것으로 기록되어 있다.
- **현재 보존 상태:** `src/` 폴더, `index.md`, `README.md`는 모두 루트에 존재한다. `src/`에는 다수의 Python 게임 파일이 유지되어 있으며, README는 실험 기록과 자동화 프롬프트를 보존한다.

### 모델 및 실행 환경 확인 기록
- **에이전트:** MistClaw / OpenClaw cron 기반 자동화.
- **실제 cron payload 모델:** `ollama/gemma4:latest` (`auto-game-developer-cat`, `auto-git-push-cat` 양쪽 모두 동일).
- **서빙:** Ollama, `http://127.0.0.1:11434`.
- **모델 메타데이터:** `ollama show gemma4:latest --verbose` 기준 `architecture=gemma4`, `parameters=8.0B`, `quantization=Q4_K_M`, `context length=131072`.
- **OpenClaw 모델 설정:** `agents/main/agent/models.json` 및 `agents/gemma/agent/models.json`에서 `ollama/gemma4:latest`의 `contextWindow`는 `200000`, `maxTokens`는 `8192`로 기록되어 있다.
- **컨텍스트 관련 주의:** 이 저장소의 실험 설명과 사용자 기억에는 “4K 컨텍스트 창의 Gemma 4 E4B”라는 운용 인식이 있으나, 현재 남아 있는 `.openclaw-gemma` 설정 파일에서는 `4096`, `4k`, `num_ctx` 같은 명시적 4K 제한 설정을 찾지 못했다. 따라서 본 README에는 “사용자가 의도/기억한 최소 컨텍스트 운용”과 “현재 보존된 설정/모델 메타데이터”를 구분해 기록한다.
- **해석:** 실제 결과물은 대형 클라우드 모델이 아니라, Ollama 로컬 서빙의 Gemma 4 8B Q4_K_M 모델이 cron 루틴을 통해 반복적으로 생성/커밋한 산물로 보존한다.

---
본 저장소는 모든 커밋과 파일 생성은 에이전트의 자율 작동 결과물을 포함합니다.
